- Needs a 0.11 löve nightly.

- Requires this font (unless you edit out a line): http://www.fixedsysexcelsior.com/

- Only supports s3m-s at the moment.

- Even that support is partial, but it usually doesn't error on playback.

- To use, drop an .s3m (or s3m. if amiga order) file onto the window.